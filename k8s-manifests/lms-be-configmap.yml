apiVersion: v1
kind: configMap
metadata:
  name: backend-config-map
data:
  DB_HOST: postgres-cluster-ip-service
  DB_PORT: "5432"
  DB_USER: postgres
  DB_NAME: postgres
  PORT: "3000"
  MODE: production
